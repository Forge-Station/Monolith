<controls:FancyWindow xmlns="https://spacestation14.io"
                    xmlns:controls="clr-namespace:Content.Client.UserInterface.Controls"
                    xmlns:ui="clr-namespace:Content.Client.Shuttles.UI"
                    Title="{Loc 'drone-console-title'}"
                    SetSize="1000 600"
                    MinSize="800 500">
    <BoxContainer Orientation="Horizontal"
                  HorizontalExpand="True"
                  VerticalExpand="True">

        <BoxContainer Orientation="Vertical"
                      MinWidth="250"
                      MaxWidth="250"
                      HorizontalExpand="False"
                      VerticalExpand="True"
                      Margin="5">
            <controls:StripeBack>
                <Label Text="{Loc 'drone-console-list'}" FontColorOverride="LightGray" Align="Center"/>
            </controls:StripeBack>
            <ScrollContainer VerticalExpand="True">
                <BoxContainer Name="DroneListContainer" Orientation="Vertical"/>
            </ScrollContainer>

            <BoxContainer Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Name="SelectAllBtn" Text="{Loc 'drone-console-select-all'}" Margin="2"/>
                <Button Name="DeselectAllBtn" Text="{Loc 'drone-console-clear-select'}" Margin="2"/>
            </BoxContainer>
        </BoxContainer>

        <!-- Mono - wrap: LayoutContainer + Wide so radar mode buttons can inject and the map fills leftover space. -->
        <LayoutContainer HorizontalExpand="True"
                         VerticalExpand="True"
                         Margin="5 4 10 5">
            <ui:ShuttleNavControl Name="NavRadar"
                                  HorizontalExpand="True"
                                  VerticalExpand="True"
                                  VerticalAlignment="Stretch"/>
        </LayoutContainer>

    </BoxContainer>
</controls:FancyWindow>
