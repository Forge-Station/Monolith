<controls:FancyWindow xmlns="https://spacestation14.io"
                      xmlns:controls="clr-namespace:Content.Client.UserInterface.Controls"
                      Title="{Loc 'salvage-expedition-window-title'}"
                      MinSize="1080 640">
    <!-- Forge-Change: MinSize was 800 360. Wider so the claim button is on screen, taller so cards are not crushed. -->
    <BoxContainer Orientation="Vertical">
        <BoxContainer Orientation="Horizontal">
            <Label Name="NextOfferLabel"
                   Text="{Loc 'salvage-expedition-window-next'}"
                   Margin="5"></Label>
            <ProgressBar Name="NextOfferBar"
                         HorizontalExpand="True"
                         MinValue="0"
                         MaxValue="1"
                         SetHeight="25"/>
            <Label Name="NextOfferText" Text="0.00"
                   Margin="5"/>
        </BoxContainer>
        <controls:HLine Color="#404040" Thickness="2" Margin="0 5 0 5"/>
        <!-- Forge-Change-Start: difficulty columns scroll sideways instead of being clipped. -->
        <ScrollContainer HScrollEnabled="True"
                         VScrollEnabled="False"
                         HorizontalExpand="True"
                         VerticalExpand="True"
                         ScrollSpeedX="160">
            <BoxContainer Name="Container"
                          Orientation="Horizontal"
                          Margin="5 0 5 0"/>
        </ScrollContainer>
        <!-- Forge-Change-End -->
        <!-- Forge-Change: MinHeight so a short window cannot squash this button into an invalid style box. -->
        <Button Name="Finish"
                Text="{Loc 'salvage-expedition-window-finish'}"
                HorizontalAlignment="Right"
                MinHeight="32"/>
    </BoxContainer>
</controls:FancyWindow>
